// Extracted from the former 45KB single-file App.tsx.

import { Icon } from '../components/common/Icon'

export function PlaylistHome({ playlists, onCreate, onOpen, onDelete, onRename }: { playlists: PlaylistRecord[]; onCreate: () => void; onOpen: (id: string) => void; onDelete: (id: string) => void; onRename: (id: string) => void }) { return <section className="card-grid-page"><div className="page-intro"><span className="kicker">YOUR MIXES</span><h3>Playlists that feel like places.</h3><p>Keep a few intentional collections close by. Smart lists stay current as your library changes.</p></div><button className="new-card" onClick={onCreate}><Icon name="plus" size={24} /><strong>New playlist</strong><span>Start a fresh listening room</span></button>{playlists.map(playlist => <div className="playlist-card" key={playlist.id}><button onClick={() => onOpen(playlist.id)}><span className="playlist-art"><Icon name="playlist" size={26} /></span><strong>{playlist.name}</strong><span>{playlist.trackIds.length} tracks</span></button><button className="text-link" onClick={() => onRename(playlist.id)}>Rename</button><button className="delete-quiet" onClick={() => { if (window.confirm(`Delete the playlist “${playlist.name}”? Its songs stay in your library.`)) onDelete(playlist.id) }} aria-label={`Delete ${playlist.name}`}><Icon name="close" /></button></div>)}</section> }
